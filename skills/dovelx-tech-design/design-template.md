# 技术设计文档模板

生成技术设计文档时使用此模板。路径：`.claude/doc/<功能名>/design-<日期>-v2.md`

```markdown
# 技术方案：<功能名>

**设计日期**：YYYY-MM-DD
**关联 PRD**：`.claude/doc/<功能名>/design-<日期>-v1.md`

## 1. 方案概述

[一句话描述实现思路]

## 2. 涉及模块

| 模块 | 变更内容 | 影响范围 |
|------|---------|---------|
| 模块 A | 新增/修改/删除 | 高/中/低 |

## 3. 架构 / 数据流

[模块交互说明，文字或 ASCII 图]

```
请求 → Controller → Service → Repository → Database
                 ↓
            第三方服务
```

## 4. API 设计

### POST /api/v1/xxx
- **描述**：...
- **权限**：...
- **请求体**：
  ```json
  {
    "field": "type"
  }
  ```
- **响应体**：
  ```json
  {
    "code": 0,
    "data": {},
    "message": "success"
  }
  ```
- **错误码**：
  | 错误码 | 含义 |
  |--------|------|
  | 400 | 参数错误 |
  | 403 | 无权限 |

### GET /api/v1/xxx/:id
- **描述**：...

## 5. 数据库 Schema

[新增/变更的表结构，使用项目当前数据库方言]

```sql
-- 新增表
CREATE TABLE xxx (
    id          BIGINT       PRIMARY KEY,
    name        VARCHAR(255) NOT NULL,
    created_at  DATETIME     NOT NULL,
    updated_at  DATETIME     NOT NULL
);

-- 新增索引
CREATE INDEX idx_xxx_name ON xxx(name);
```

**变更说明**：
- 新增字段：...
- 索引变更：...
- 迁移注意事项：...

## 6. 关键实现步骤

- [ ] 数据库迁移脚本
- [ ] 后端 Repository 层
- [ ] 后端 Service 层业务逻辑
- [ ] 后端 Controller / API 层
- [ ] 前端数据模型 / Store
- [ ] 前端页面 / 组件
- [ ] 单元测试
- [ ] 集成测试

## 7. 技术决策记录（ADR）

| 决策点 | 备选方案 | 最终选择 | 选择理由 |
|--------|---------|---------|---------|
| ... | 方案A vs 方案B | 方案A | ... |

## 8. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| ... | 高/中/低 | 高/中/低 | ... |

## 9. 非功能设计

- **性能**：...（预期 QPS、响应时间、缓存策略）
- **安全**：...（鉴权方式、数据脱敏、输入校验）
- **监控 / 告警**：...（关键指标、告警阈值）
```
